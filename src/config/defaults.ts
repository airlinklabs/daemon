/**
 * Container resource defaults — used by start, crash-restart, backup-restore.
 */

export {
  DEFAULT_MEMORY_MB,
  DEFAULT_CPU_PERCENT,
  DEFAULT_STORAGE_MB,
  DEFAULT_SWAP_MB,
  INSTALLER_MEMORY_MB,
  INSTALLER_CPU_PERCENT,
} from "./limits";
