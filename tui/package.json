{
  "name": "airlinkd-tui",
  "private": true,
  "version": "3.0.2",
  "description": "Airlink Daemon TUI - host resources, containers, and live daemon logs",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run --watch src/index.ts"
  },
  "dependencies": {
    "@opentui/core": "^0.4.5"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0"
  }
}
