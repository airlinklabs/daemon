key=key
port=3002
DEBUG=false
version=1.0.0
environment=development
STATS_INTERVAL=10000
# HMAC is required by default; set REQUIRE_HMAC=false only for legacy panels that cannot sign requests.
# REQUIRE_HMAC=false
